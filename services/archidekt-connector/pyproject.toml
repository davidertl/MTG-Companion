[project]
name = "mancutg-backend-archidekt-connector"
version = "0.1.0"
description = "Read-only Archidekt connector for MancuTG-backend in the MancuTG-Companion project"
requires-python = ">=3.12"
dependencies = [
  "pyrchidekt",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
